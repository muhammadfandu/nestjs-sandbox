<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quality Control Issues Report</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            color: #1a1a1a;
            line-height: 1.6;
        }

        .page {
            page-break-after: always;
            padding: 40px;
            min-height: 100vh;
        }

        .page:last-child {
            page-break-after: auto;
        }

        .issue-header {
            margin-bottom: 30px;
        }

        .issue-number-row {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 8px;
        }

        .issue-number {
            font-size: 28px;
            font-weight: 600;
            color: #4a5568;
        }

        .status-badge {
            display: inline-block;
            padding: 6px 16px;
            background-color: #4a5568;
            color: white;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .issue-title {
            font-size: 32px;
            font-weight: 700;
            color: #1a1a1a;
            margin-bottom: 20px;
            line-height: 1.2;
        }

        .meta-info {
            display: flex;
            gap: 30px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .meta-item {
            display: flex;
            align-items: center;
            gap: 8px;
            color: #4a5568;
            font-size: 16px;
        }

        .meta-icon {
            width: 20px;
            height: 20px;
            fill: currentColor;
        }

        .section {
            margin-bottom: 30px;
        }

        .section-title {
            font-size: 18px;
            font-weight: 700;
            color: #1a1a1a;
            margin-bottom: 12px;
        }

        .description-text {
            font-size: 16px;
            color: #4a5568;
            line-height: 1.6;
        }

        .assignees {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
        }

        .assignee {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .assignee-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #e2e8f0;
        }

        .assignee-avatar-placeholder {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: #cbd5e0;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            color: white;
            font-size: 16px;
        }

        .assignee-name {
            font-size: 16px;
            font-weight: 500;
            color: #1a1a1a;
        }

        .photos-section {
            margin-bottom: 30px;
        }

        .photos-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
        }

        .photo-column {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .photo-label {
            font-size: 18px;
            font-weight: 700;
            color: #1a1a1a;
            margin-bottom: 10px;
        }

        .photo-item {
            background: #f7fafc;
            border-radius: 12px;
            overflow: hidden;
            border: 1px solid #e2e8f0;
            margin-bottom: 15px;
        }

        .photo-image {
            width: 100%;
            max-height: 300px;
            object-fit: cover;
            display: block;
        }

        .photo-placeholder {
            width: 100%;
            height: 200px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #edf2f7;
            color: #718096;
            font-size: 14px;
            text-align: center;
            padding: 20px;
        }

        .photo-caption {
            padding: 12px 16px;
            font-size: 13px;
            color: #718096;
            background: white;
        }

        .divider {
            height: 1px;
            background: #e2e8f0;
            margin: 30px 0;
        }

        @media print {
            .page {
                page-break-after: always;
            }
            
            .page:last-child {
                page-break-after: auto;
            }
        }
    </style>
</head>
<body>
    {{#each issues}}
    <div class="page">
        <!-- Issue Header -->
        <div class="issue-header">
            <div class="issue-number-row">
                <div class="issue-number">Issue #{{padNumber @index}}</div>
                <span class="status-badge">{{formatStatus status}}</span>
            </div>
            <h1 class="issue-title">{{title}}</h1>
        </div>

        <!-- Meta Information -->
        <div class="meta-info">
            <div class="meta-item">
                <svg class="meta-icon" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd"/>
                </svg>
                <span>{{location}}</span>
            </div>
            <div class="meta-item">
                <svg class="meta-icon" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"/>
                </svg>
                <span>Reported: {{formatDate issueDate}}</span>
            </div>
        </div>

        <div class="divider"></div>

        <!-- Description -->
        <div class="section">
            <h2 class="section-title">Description:</h2>
            <p class="description-text">{{description}}</p>
        </div>

        <!-- Assignees -->
        {{#if assignees}}
        <div class="section">
            <h2 class="section-title">Assignee:</h2>
            <div class="assignees">
                {{#each assignees}}
                <div class="assignee">
                    {{#if userImgUrl}}
                    <img src="{{userImgUrl}}" alt="{{userName}}" class="assignee-avatar">
                    {{else}}
                    <div class="assignee-avatar-placeholder">{{getInitials userName}}</div>
                    {{/if}}
                    <span class="assignee-name">{{userName}}</span>
                </div>
                {{/each}}
            </div>
        </div>
        {{/if}}

        <!-- Photos Section -->
        <div class="photos-section">
            <div class="photos-grid">
                <!-- Before Photos -->
                <div class="photo-column">
                    <h3 class="photo-label">Before Photo</h3>
                    {{#each (filterByType issueDocumentation "new")}}
                    <div class="photo-item">
                        {{#if (isImage fileType)}}
                        <img src="{{fileUrl}}" alt="{{fileName}}" class="photo-image">
                        {{else}}
                        <div class="photo-placeholder">Attachment can't be loaded</div>
                        {{/if}}
                        <div class="photo-caption">
                            Uploaded by {{userName}} on {{formatDate dateCreated}}
                        </div>
                    </div>
                    {{else}}
                    <div class="photo-item">
                        <div class="photo-placeholder">No before photos available</div>
                    </div>
                    {{/each}}
                </div>

                <!-- After Photos -->
                <div class="photo-column">
                    <h3 class="photo-label">After Photo</h3>
                    {{#each (filterByType issueDocumentation "updated")}}
                    <div class="photo-item">
                        {{#if (isImage fileType)}}
                        <img src="{{fileUrl}}" alt="{{fileName}}" class="photo-image">
                        {{else}}
                        <div class="photo-placeholder">Attachment can't be loaded</div>
                        {{/if}}
                        <div class="photo-caption">
                            Uploaded by {{userName}} on {{formatDate dateCreated}}
                        </div>
                    </div>
                    {{else}}
                    <div class="photo-item">
                        <div class="photo-placeholder">No after photos available</div>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
    {{/each}}
</body>
</html>
